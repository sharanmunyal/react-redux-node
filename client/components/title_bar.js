require('../stylesheets/title_bar.css');

import React from 'react';
import { Component } from 'react';

export default class TitleBar extends Component {
    render() {
        return (
            <div>Title for app</div>
        );
    }
}